# InfluFind - Система поиска инфлюенсеров в Instagram

**InfluFind** - это веб-приложение для автоматизированного поиска и анализа инфлюенсеров в Instagram по географическим локациям. Система позволяет находить активных пользователей в конкретных городах и регионах, анализировать их аудиторию и экспортировать данные для маркетинговых кампаний.

## 🚀 Основные возможности

### 🔍 Поиск инфлюенсеров
- **Географический поиск**: Поиск по странам, городам и конкретным локациям
- **Умные фильтры**: Фильтрация по количеству подписчиков, просмотрам Reels, активности
- **Многоуровневый парсинг**: Поиск через посты в локациях Instagram
- **Кэширование результатов**: Быстрый доступ к уже найденным данным

### 📊 Анализ данных
- **Профили инфлюенсеров**: Подписчики, активность, биография, аватар
- **Статистика по локациям**: Количество активных пользователей в каждой локации
- **Экспорт данных**: Копирование списков username для дальнейшего использования
- **Обновление профилей**: Актуализация данных по конкретным инфлюенсерам

### 🛡️ Безопасность и стабильность
- **Гостевой режим**: Парсинг без авторизации для базовых задач
- **Прокси поддержка**: HTTP, HTTPS, SOCKS5 прокси для обхода ограничений
- **Anti-detection**: Продвинутые техники маскировки автоматизации
- **Управление сессиями**: Сохранение и переиспользование авторизации Instagram

## 🏗️ Архитектура проекта

### Backend (Node.js + TypeScript)
```
backend/
├── src/
│   ├── routes/          # API маршруты
│   │   ├── authRoutes.ts       # Авторизация пользователей
│   │   ├── instagramRoutes.ts  # Подключение Instagram
│   │   ├── locationRoutes.ts   # Управление локациями
│   │   ├── proxyRoutes.ts      # Настройка прокси
│   │   └── searchRoutes.ts     # Поиск инфлюенсеров
│   ├── parsers/         # Парсеры Instagram
│   │   ├── locationParser.ts   # Основной парсер
│   │   ├── postParser.ts       # Парсинг постов
│   │   ├── manualAuth.ts       # Ручная авторизация
│   │   └── locationHierarchy.ts # Иерархия локаций
│   ├── services/        # Бизнес-логика
│   │   ├── userService.ts      # Управление пользователями
│   │   └── cacheService.ts     # Кэширование
│   ├── utils/           # Утилиты
│   │   ├── cookieManager.ts    # Управление cookies
│   │   └── helpers.ts          # Вспомогательные функции
│   └── middleware/      # Промежуточное ПО
└── data/               # База данных (JSON файлы)
    ├── users.json
    └── locations/      # Локации по странам и городам
```

### Frontend (React + TypeScript)
```
frontend/
├── src/
│   ├── components/      # React компоненты
│   │   ├── Auth/              # Авторизация
│   │   ├── SearchForm.tsx     # Основная форма поиска
│   │   ├── InstagramConnect/  # Подключение Instagram
│   │   └── ProxyConnect/      # Настройка прокси
│   ├── contexts/        # React контексты
│   │   └── AuthContext.tsx    # Контекст авторизации
│   ├── hooks/           # Кастомные хуки
│   │   └── useSearchForm.ts   # Логика формы поиска
│   ├── types/           # TypeScript типы
│   └── utils/           # Утилиты фронтенда
```

## 🔧 Технический стек

### Backend
- **Node.js** - Серверная платформа
- **Express** - Веб-фреймворк
- **TypeScript** - Типизированный JavaScript
- **Puppeteer** - Автоматизация браузера
- **JWT** - Аутентификация
- **bcryptjs** - Хеширование паролей

### Frontend
- **React 19** - UI библиотека
- **TypeScript** - Типизация
- **CSS Modules** - Стилизация
- **Axios** - HTTP клиент

### База данных
- **JSON файлы** - Легковесное хранение данных
- **Файловая система** - Простое развертывание

## 📋 Установка и запуск

### Предварительные требования
- Node.js 16+
- npm или yarn
- Chrome/Chromium браузер

### Клонирование репозитория
```bash
git clone https://github.com/yourusername/influ-find.git
cd influ-find
```

### Установка зависимостей

**Backend:**
```bash
cd backend
npm install
```

**Frontend:**
```bash
cd frontend
npm install
```

### Настройка окружения

Создайте файл `.env` в папке backend:
```env
PORT=3001
JWT_SECRET=your-secret-key-here
NODE_ENV=development
```

### Запуск проекта

**Backend (терминал 1):**
```bash
cd backend
npm run dev
```

**Frontend (терминал 2):**
```bash
cd frontend
npm start
```

Приложение будет доступно по адресу: `http://localhost:3000`

## 🎯 Пошаговое использование

### 1. Регистрация и авторизация
- Зарегистрируйтесь в системе или войдите с существующими данными
- Все данные хранятся локально в JSON файлах

### 2. Настройка подключений (опционально)
- **Прокси**: Настройте HTTP/HTTPS/SOCKS5 прокси для обхода ограничений
- **Instagram**: Подключите аккаунт Instagram для расширенных возможностей

### 3. Парсинг географических данных
- Выберите страну из списка
- Если городов нет - нажмите "Спарсить города"
- Выберите конкретный город
- Если локаций нет - нажмите "Спарсить локации"

### 4. Поиск инфлюенсеров
- Выберите интересующие локации (галочками)
- Настройте фильтры (подписчики, просмотры Reels)
- Выберите режим поиска:
  - **"Показать из базы"** - быстрый поиск в кэше
  - **"Продолжить парсинг"** - продолжение с последнего места
  - **"Спарсить новых"** - полный перепарсинг

### 5. Работа с результатами
- Просмотр найденных инфлюенсеров
- Обновление профилей кнопкой "Обновить"
- Копирование никнеймов одним кликом
- Применение фильтров к результатам

## 🔧 API Endpoints

### Авторизация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход в систему
- `GET /api/auth/me` - Получение данных пользователя

### Локации
- `GET /api/locations/countries` - Список стран
- `GET /api/locations/cities/:countryCode` - Города страны
- `GET /api/locations/locations/:cityId` - Локации города
- `POST /api/locations/parse-cities` - Парсинг городов

### Поиск
- `POST /api/search/locations` - Поиск по локациям
- `POST /api/search/city` - Парсинг локаций города
- `POST /api/search/profile` - Обновление профиля

### Подключения
- `POST /api/proxy/connect` - Подключение прокси
- `POST /api/instagram/connect` - Подключение Instagram

## ⚙️ Конфигурация

### Настройки парсера
```typescript
// backend/src/config/parser.ts
export const PARSER_CONFIG = {
  delays: {
    pageLoad: 3000,      // Задержка загрузки страницы
    betweenRequests: 2000, // Между запросами
    betweenCities: 5000   // Между городами
  },
  limits: {
    maxPagesPerCity: 10,  // Максимум страниц на город
    maxPostsPerLocation: 100, // Максимум постов на локацию
    requestsBeforeCooldown: 50 // Запросов до паузы
  }
};
```

### Безопасность
- Все пароли хешируются с помощью bcrypt
- JWT токены для авторизации
- Валидация всех входных данных
- Защита от XSS и CSRF атак

## 🚨 Ограничения и рекомендации

### Технические ограничения
- **Rate limiting**: Instagram ограничивает количество запросов
- **IP блокировка**: Использование прокси рекомендуется
- **Captcha**: Может потребоваться ручное решение

### Рекомендации по использованию
- **Прокси**: Используйте мобильные IP адреса
- **Аккаунты**: Меняйте аккаунты Instagram периодически  
- **Задержки**: Не уменьшайте задержки между запросами
- **Объемы**: Парсите небольшими батчами (до 50 локаций)

### Этические принципы
- Соблюдайте Terms of Service Instagram
- Не используйте данные для спама
- Уважайте приватность пользователей
- Используйте только для легальных целей

## 🐛 Отладка и логирование

### Логи backend
```bash
# Запуск с детальными логами
DEBUG=* npm run dev

# Логи сохраняются в консоль с временными метками
[2025-01-26T10:30:45.123Z] [INFO] 🔍 Начинаем поиск инфлюенсеров...
```

### Частые проблемы
1. **"Instagram не подключен"** - Подключите аккаунт в настройках
2. **"Прокси не работает"** - Проверьте данные прокси-сервера
3. **"Города не найдены"** - Запустите парсинг городов
4. **"Локации не найдены"** - Запустите парсинг локаций

## 🤝 Участие в разработке

### Структура веток
- `main` - Стабильная версия
- `develop` - Разработка новых функций
- `feature/*` - Отдельные фичи

### Код-стайл
- ESLint + Prettier для JavaScript/TypeScript
- Комментарии на русском языке
- Логирование через утилиту log()

### Тестирование
```bash
# Backend тесты
cd backend && npm test

# Frontend тесты  
cd frontend && npm test
```

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

## ⚠️ Дисклеймер

Этот проект создан исключительно в образовательных целях. Пользователи несут полную ответственность за соблюдение условий использования Instagram и применимого законодательства. Авторы не несут ответственности за неправомерное использование данного программного обеспечения.

---

**Версия**: 1.0.0  
**Последнее обновление**: Январь 2025  
**Поддержка**: [GitHub Issues](https://github.com/yourusername/influ-find/issues)
